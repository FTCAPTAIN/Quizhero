import React from 'react';

const AiExplanationsScreen: React.FC = () => {
  // This component is no longer used and has been deprecated.
  // The AI explanation feature is now integrated directly into the ResultsScreen.
  return null;
};

export default AiExplanationsScreen;